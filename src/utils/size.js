import {Dimensions} from 'react-native';

export const MAX_WIDTH = Dimensions.get('screen').width;
export const MAX_HEIGHT = Dimensions.get('screen').height;
export const GAP_SIZE = 270; // gap between the two parts of the pipe
export const PIPE_WIDTH = 80; // width of the pipe
